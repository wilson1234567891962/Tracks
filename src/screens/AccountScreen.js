import React from "react";
import { StyleSheet, View, Text } from "react-native";

const AccountScren = () => {
    return <Text>Account Screen</Text>
};

const styles = StyleSheet.create({});

export default AccountScren;