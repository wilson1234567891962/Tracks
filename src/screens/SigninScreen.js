import React from "react";
import { StyleSheet, View, Text } from "react-native";

const SigninScreen = () => {
    return <Text>SigninScreen Screen</Text>
};

const styles = StyleSheet.create({});

export default SigninScreen;