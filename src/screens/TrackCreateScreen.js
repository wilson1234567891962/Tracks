import React from "react";
import { StyleSheet, View, Text } from "react-native";

const TrackCreateScreen = () => {
  return <Text>TrackCreateScreen Screen</Text>;
};

const styles = StyleSheet.create({});

export default TrackCreateScreen;